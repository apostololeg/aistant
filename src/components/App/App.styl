.root
  position relative
  transition opacity .3s ease-out

  &::after
    content ''
    position absolute
    top 0
    left 0
    right 0
    bottom 0
    background-color var(--decent-color-alpha-100)
    z-index 1

    .connected&
      opacity 0
      pointer-events none

